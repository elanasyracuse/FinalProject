# RAG Research Bot - Requirements File
# Python 3.10+ required

# Core dependencies
arxiv==2.1.3              # For fetching papers from arXiv
pypdf==3.17.4             # For parsing PDF documents (PyPDF2 is deprecated)
numpy==1.26.4             # For vector operations and similarity calculations

# OpenAI API for embeddings
openai>=1.12.0            # OpenAI API client for embeddings and chat completions

# Database and storage
# sqlite3 is included in Python standard library

# Configuration and secrets
toml==0.10.2              # For reading secrets.toml configuration

# Scheduling and automation
schedule==1.2.0           # For scheduling periodic pipeline runs

# Web UI framework
streamlit>=1.31.0         # For building the web interface

# Optional but recommended
python-dotenv==1.0.1      # For loading environment variables from .env file
pandas>=2.0.3             # Useful for data manipulation in Streamlit UI
plotly>=5.18.0            # For creating interactive visualizations in UI

# Knowledge graph dependencies (for graph_module.py)
neo4j==6.0.3             # Neo4j database driver for knowledge graphs
graphiti-core==0.24.0            # Graphiti core library for graph operations
pyvis==0.3.2                    # Network visualization library

# Development tools (optional)
ipython==8.12.3           # Enhanced Python shell for debugging
black==23.12.1            # Code formatter
pylint==3.0.3             # Code linter
